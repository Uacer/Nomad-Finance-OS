{
  "name": "koala-ledger",
  "version": "0.1.0",
  "private": true,
  "description": "Koala Ledger MVP backend",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "agentation:server": "agentation-mcp server --port 4747",
    "test": "node --test",
    "generate:monthly-review": "node src/review-job.js",
    "backup:db": "node src/backup-db.js",
    "restore:db": "node src/restore-db.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "better-sqlite3": "^11.8.1",
    "express": "^4.21.2",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "agentation-mcp": "^1.2.0",
    "supertest": "^7.0.0"
  }
}
